version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd my-portfolio
        - npm ci
    build:
      commands:
        - cd my-portfolio
        - npm run build
  artifacts:
    baseDirectory: my-portfolio/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
